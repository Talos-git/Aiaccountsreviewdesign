
  # Base app with MUI

  This is a code bundle for Base app with MUI. The original project is available at https://www.figma.com/design/nzXGA5NQMlPu2wxSAEetOe/Base-app-with-MUI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  